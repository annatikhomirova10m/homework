//Число простых делителей
#include "pch.h"
#include <iostream>
#include <string>
using namespace std;
int countDivider(int X) {
	int Resilt = 1;
	int valX = X;
	int cntDiv = 0;
	while (valX > 1) {
		for (int i = 2; i <= valX; i++) {
			if (valX % i == 0) {
				cntDiv = cntDiv + 1;
				Res = i;
				valX = valX / i;
				break;
			}
		}
	}
	if (Result == X) { cntDiv = cntDiv - 1; }
	return cntDiv;
}
